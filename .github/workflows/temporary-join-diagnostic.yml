name: Temporary join diagnostic

on:
  workflow_dispatch:
    inputs:
      operation:
        description: Deploy or delete the guarded diagnostic
        required: true
        type: choice
        options: [deploy, delete]

permissions:
  contents: read

jobs:
  maintain:
    runs-on: ubuntu-latest
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_PROJECT_ID: ${{ vars.SUPABASE_PROJECT_ID }}
    steps:
      - uses: actions/checkout@v4
      - uses: supabase/setup-cli@v1
        with:
          version: 2.115.0
      - name: Deploy guarded diagnostic
        if: inputs.operation == 'deploy'
        run: supabase functions deploy diagnose-join-application --project-ref "$SUPABASE_PROJECT_ID" --no-verify-jwt
      - name: Delete guarded diagnostic
        if: inputs.operation == 'delete'
        run: supabase functions delete diagnose-join-application --project-ref "$SUPABASE_PROJECT_ID" --yes
