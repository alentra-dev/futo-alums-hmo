name: Temporary 2026 payment reset

on:
  workflow_dispatch:
    inputs:
      operation:
        description: Deploy or delete the guarded reset function
        required: true
        type: choice
        options: [deploy, delete]

permissions:
  contents: read

jobs:
  maintain:
    runs-on: ubuntu-latest
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_PROJECT_ID: ${{ vars.SUPABASE_PROJECT_ID }}
    steps:
      - uses: actions/checkout@v4
      - uses: supabase/setup-cli@v1
        with:
          version: 2.115.0
      - name: Deploy guarded reset function
        if: inputs.operation == 'deploy'
        run: supabase functions deploy reset-2026-refunded-payments --project-ref "$SUPABASE_PROJECT_ID" --no-verify-jwt
      - name: Delete guarded reset function
        if: inputs.operation == 'delete'
        run: supabase functions delete reset-2026-refunded-payments --project-ref "$SUPABASE_PROJECT_ID" --yes
